// first we define a list of origin or domain that we want to have access to our api
const allowedOrigins = ['http://127.0.0.1:5500', 
                    'http://localhost:3500', 
                    'http://www.google.com', 
                    'http://localhost:3000'
];

module.exports = allowedOrigins;